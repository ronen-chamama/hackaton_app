/**
 * Student route group layout.
 * Phase 2 will add: active hackathon fetch, group resolution,
 * realtime subscription setup, and the full page shell.
 */
export default function StudentLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
